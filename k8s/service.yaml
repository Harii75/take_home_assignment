apiVersion: v1
kind: Service
metadata:
  name: rest-service
  namespace: hello-world
  labels:
    app: rest-service
spec:
  type: NodePort
  selector:
    app: rest-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080
